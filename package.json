{
  "name": "zero-inbox-cats-for-gmail",
  "version": "0.12.0",
  "description": "Adds a random cat to the empty state of Gmail inbox",
  "main": "index.js",
  "author": "mordech",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/firefox-webext-browser": "^94.0.1",
    "lit-html": "^2.4.0",
    "web-ext": "^7.4.0"
  },
  "scripts": {
    "build:firefox": "rsync manifest/firefox.json src/manifest.json && web-ext build -s ./src/ -a ./dist/firefox/ -o -i=\"**/*.map\" -i=\"**/*.ts\" -i=\"**/@types\"",
    "build:chrome": "rsync manifest/chrome.json src/manifest.json && web-ext build -s ./src/ -a ./dist/chrome/ -o -i=\"**/*.map\" -i=\"**/*.ts\" -i=\"**/@types\"",
    "build": "yarn build:firefox && npm run build:chrome",
    "start": "rsync manifest/firefox.json src/manifest.json && yarn web-ext run --source-dir ./src/ -t firefox-desktop -t chromium --watch-file -u https://mail.google.com/mail/u/0/#inbox",
    "lint": "web-ext lint -s ./src/"
  }
}
